{
  "name": "paperbuddy-school-fintech",
  "version": "1.0.0",
  "private": true,
  "description": "PaperBuddy modern school fee management platform",
  "scripts": {
    "dev": "npm -w frontend run dev",
    "dev:api": "npm -w backend run dev",
    "build": "npm -w frontend run build && npm -w backend run build",
    "lint": "npm -w frontend run lint && npm -w backend run lint",
    "prisma:generate": "npm -w backend run prisma:generate"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "overrides": {
    "postcss": "^8.5.25",
    "sharp": "^0.35.3"
  }
}
